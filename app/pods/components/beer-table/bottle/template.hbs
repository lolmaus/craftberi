{{#ta-ble
  columns   = columns
  rows      = (append 'header' (sort-by 'brewery.name' 'name' beers))
  rowClass  = (concat componentCssClassName '-row')
  cellClass = (concat componentCssClassName '-cell')
  as |beer column|
}}
  {{#if (eq column.slug 'name')}}
    {{#if (eq beer 'header')}}
      Название
    {{else}}
      {{strip-tags beer.name}}
    {{/if}}

  {{else if (eq column.slug 'style')}}
    {{#if (eq beer 'header')}}
      Сорт
    {{else}}
      {{#if beer.styleExplanation}}
        <abbr title="{{strip-tags beer.styleExplanation}}">{{strip-tags beer.style}}</abbr>
      {{else}}
        {{strip-tags beer.style}}
      {{/if}}
    {{/if}}

  {{else if (eq column.slug 'brewery')}}
    {{#if (eq beer 'header')}}
      Пивоварня
    {{else}}
      {{strip-tags beer.brewery.name}}
    {{/if}}

  {{else if (eq column.slug 'ibu')}}
    {{#if (eq beer 'header')}}
      <abbr title = "Расчетная горечь">IBU</abbr>
    {{else}}
      {{beer.ibu}}
    {{/if}}

  {{else if (eq column.slug 'abv')}}
    {{#if (eq beer 'header')}}
      <abbr title = "Крепость">ABV</abbr>
    {{else}}
      {{beer.abv}}°
    {{/if}}

  {{else if (eq column.slug 'og')}}
    {{#if (eq beer 'header')}}
      <abbr title = "Плотность">OG</abbr>
    {{else}}
      {{#if beer.gravity}}
        {{beer.gravity}}%
      {{/if}}
    {{/if}}

  {{else if (eq column.slug 'price')}}
    {{#if (eq beer 'header')}}
      <abbr title = "Цена за бокал">₽/🍾</abbr>
    {{else}}
      {{beer.priceBottle}} ₽
    {{/if}}
  {{/if}}
{{/ta-ble}}

