{{#ta-ble
  columns   = columns
  rows      = (append 'header' (sort-by 'number' taps))
  rowClass  = (concat componentCssClassName '-row')
  cellClass = (concat componentCssClassName '-cell')
  as |tap column|
}}
  {{#if (eq column.slug 'number')}}
    {{#if (eq tap 'header')}}
      #
    {{else}}
      {{tap.number}}
    {{/if}}

  {{else if (eq column.slug 'name')}}
    {{#if (eq tap 'header')}}
      Название
    {{else}}
      {{strip-tags tap.beer.name}}
    {{/if}}

  {{else if (eq column.slug 'style')}}
    {{#if (eq tap 'header')}}
      Сорт
    {{else}}
      {{#if tap.beer.styleExplanation}}
        <abbr title="{{strip-tags tap.beer.styleExplanation}}">{{strip-tags tap.beer.style}}</abbr>
      {{else}}
        {{strip-tags tap.beer.style}}
      {{/if}}
    {{/if}}

  {{else if (eq column.slug 'brewery')}}
    {{#if (eq tap 'header')}}
      Пивоварня
    {{else}}
      {{strip-tags tap.beer.brewery.name}}
    {{/if}}

  {{else if (eq column.slug 'ibu')}}
    {{#if (eq tap 'header')}}
      <abbr title = "Расчетная горечь">IBU</abbr>
    {{else}}
      {{tap.beer.ibu}}
    {{/if}}

  {{else if (eq column.slug 'abv')}}
    {{#if (eq tap 'header')}}
      <abbr title = "Крепость">ABV</abbr>
    {{else}}
      {{tap.beer.abv}}°
    {{/if}}

  {{else if (eq column.slug 'og')}}
    {{#if (eq tap 'header')}}
      <abbr title = "Плотность">OG</abbr>
    {{else}}
      {{#if tap.beer.gravity}}
        {{tap.beer.gravity}}%
      {{/if}}
    {{/if}}

  {{else if (eq column.slug 'price-pint')}}
    {{#if (eq tap 'header')}}
      <abbr title = "Цена за бокал">₽/🍺</abbr>
    {{else}}
      {{tap.beer.pricePint}} ₽
    {{/if}}

  {{else if (eq column.slug 'price-liter')}}
    {{#if (eq tap 'header')}}
      <abbr title = "Цена за литр">₽/л</abbr>
    {{else}}
      {{tap.beer.priceLiter}} ₽
    {{/if}}
  {{/if}}
{{/ta-ble}}

